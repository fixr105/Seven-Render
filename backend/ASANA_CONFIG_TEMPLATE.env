# Asana Integration Configuration
# Copy this to .env and fill in your values

# ============================================
# OPTION 1: Personal Access Token (Recommended)
# ============================================
# Get your PAT from: https://app.asana.com/0/my-apps
# Click "Create new token" and copy it here
ASANA_PAT=your_personal_access_token_here

# ============================================
# OPTION 2: OAuth 2.0 (For user-facing apps)
# ============================================
# OAuth Client ID (provided)
ASANA_CLIENT_ID=1212642476076052

# OAuth Client Secret (keep secret!)
ASANA_CLIENT_SECRET=5ab9fe0e0ecf7d33b39d60b7abbfd252

# OAuth Redirect URI (configure in Asana app settings)
# For local development:
ASANA_REDIRECT_URI=http://localhost:3001/auth/asana/callback
# For production:
# ASANA_REDIRECT_URI=https://yourdomain.com/auth/asana/callback

# OAuth Access Token (obtained after OAuth flow)
# Only needed if using OAuth instead of PAT
# ASANA_OAUTH_ACCESS_TOKEN=your_oauth_access_token_here

# ============================================
# NOTES
# ============================================
# - For server-to-server integration, use ASANA_PAT (simpler)
# - OAuth is only needed for user-facing applications
# - Client Secret must be kept private (never commit to git)
# - Access tokens must be kept private

