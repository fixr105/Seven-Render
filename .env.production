# Production build default - used when Vercel env var is not set
# For Vercel: override via Settings → Environment Variables (VITE_API_BASE_URL)
# Do not add /api; the client appends it.
VITE_API_BASE_URL=https://seven-render.fly.dev
