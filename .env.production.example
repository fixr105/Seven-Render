# Backend API Configuration (Production)
# Set this in Vercel: Settings → Environment Variables (Production + Preview).
# Do not add /api; the client appends it.
VITE_API_BASE_URL=https://seven-dash.fly.dev

# Use Backend API for Authentication
VITE_USE_API_AUTH=true

# Supabase is no longer required - remove these if present
# VITE_SUPABASE_URL=
# VITE_SUPABASE_ANON_KEY=
